tokens = {
    "1234567890-my-token": {
        expiration = "2029-01-01T00:00:00Z", # Token has to be used by this expiration date
        # This is a well-known demo token, so it stays usable only while dev-mode is enabled.
        # A real deployment needs a secret, per-deployment token with devModeOnly = false.
        devModeOnly = true
        account = {
            type = "email"
            identifier = { email = "superadmin@walt.id" }
            data = { password = "super123456" }
        }
    }
}
