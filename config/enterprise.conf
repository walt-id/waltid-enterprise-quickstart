# walt.id Enterprise Service
# Base configuration

# Base domain of this service, e.g. `waltid.cloud`
baseDomain = "enterprise.localhost"
baseSsl = false
# basePort = 80 //default port is determined by `baseSsl` (80 if false, 443 if true)
